<section class="ee-section ee-stats reveal-up">
  <div class="ee-container">
    <div class="ee-stats-grid">
      <article class="ee-card ee-stat-card">
        <strong class="ee-stat-number">{{ component.books_value ?: '6' }}</strong>
        <p>{{ component.books_label ?: trans('theme.stat_books') }}</p>
      </article>
      <article class="ee-card ee-stat-card">
        <strong class="ee-stat-number">{{ component.vocab_value ?: '1000+' }}</strong>
        <p>{{ component.vocab_label ?: trans('theme.stat_vocab') }}</p>
      </article>
      <article class="ee-card ee-stat-card">
        <strong class="ee-stat-number">{{ component.phrases_value ?: '700+' }}</strong>
        <p>{{ component.phrases_label ?: trans('theme.stat_phrases') }}</p>
      </article>
      <article class="ee-card ee-stat-card">
        <strong class="ee-stat-number">{{ component.levels_value ?: 'A1-C2' }}</strong>
        <p>{{ component.levels_label ?: trans('theme.stat_levels') }}</p>
      </article>
    </div>
  </div>
</section>
