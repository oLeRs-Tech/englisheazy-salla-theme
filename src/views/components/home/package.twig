{% set package_badge = theme.settings.get('package_badge') ?: 'The Z Package' %}
{% set package_title = theme.settings.get('package_title') ?: trans('theme.package_title') %}
{% set package_subtitle = theme.settings.get('package_subtitle') ?: trans('theme.package_subtitle_default') %}
{% set package_price = theme.settings.get('package_price') ?: '149' %}
{% set package_cta_text = theme.settings.get('package_cta_text') ?: trans('theme.package_cta') %}
{% set package_cta_url = theme.settings.get('package_cta_url') ?: '/products' %}

<section class="ee-section ee-package reveal-up">
  <div class="ee-container">
    <article class="ee-card ee-package-card">
      <div class="ee-package-copy">
        <p class="ee-chip ee-chip-dark">{{ package_badge }}</p>
        <h2>{{ package_title }}</h2>
        <p>{{ package_subtitle }}</p>
        <div class="ee-package-price">
          <span class="ee-price-currency">{{ trans('theme.currency_sar') }}</span>
          <strong>{{ package_price }}</strong>
        </div>
        <a href="{{ package_cta_url }}" class="ee-btn ee-btn-primary">{{ package_cta_text }}</a>
      </div>
      <div class="ee-package-books">
        <div class="ee-mini-book"></div>
        <div class="ee-mini-book"></div>
        <div class="ee-mini-book"></div>
        <div class="ee-mini-book"></div>
        <div class="ee-mini-book"></div>
        <div class="ee-mini-book"></div>
      </div>
    </article>
  </div>
</section>
